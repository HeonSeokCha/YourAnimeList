query SearchAnimeQuery(
    $page: Int = 1,
    $search: String) {
    Page(
        page: $page,
        perPage: 6
    ) {
        ... pageInfo
        media(
            search: $search,
            type: ANIME,
            sort: [FAVOURITES_DESC]
        ) {
            ...animeList
        }
    }
}