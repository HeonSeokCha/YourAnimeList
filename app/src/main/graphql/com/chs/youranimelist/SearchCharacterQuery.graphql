query SearchCharacterQuery(
    $page: Int = 1,
    $search: String) {
    Page(
        page: $page,
        perPage: 6
    ) {
        ... pageInfo
        characters(
            search: $search,
            sort: [FAVOURITES_DESC]
        ) {
            id
            name {
                full
                native
            }
            image {
                large
            }
            favourites
        }
    }
}