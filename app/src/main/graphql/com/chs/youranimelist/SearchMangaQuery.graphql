query SearchMangaQuery(
    $page: Int = 1,
    $search: String) {
    Page(
        page: $page,
        perPage: 6
    ) {
        pageInfo {
            total
            perPage
            currentPage
            lastPage
            hasNextPage
        }
        media(
            search: $search,
            type: MANGA,
            sort: [FAVOURITES_DESC]
        ) {
            ...animeList
        }
    }
}