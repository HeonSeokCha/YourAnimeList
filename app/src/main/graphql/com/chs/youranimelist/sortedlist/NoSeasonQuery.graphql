query NoSeasonQuery(
    $page: Int,
    $sort: MediaSort,
    $seasonYear: Int,
    $genre: String
) {
    page: Page(
        page: $page,
        perPage: 10,
    ) {
        pageInfo {
            ...pageBasicInfo
        }
        media(
            sort: [$sort],
            type: ANIME,
            seasonYear: $seasonYear,
            genre: $genre,
            isAdult: false
        ) {
            ... animeBasicInfo
        }
    }
}