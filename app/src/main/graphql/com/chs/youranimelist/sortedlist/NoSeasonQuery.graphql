query NoSeasonQuery(
    $page: Int = 1,
    $sort: MediaSort,
    $seasonYear: Int,
    $genre: String
) {
    Page(
        page: $page,
        perPage: 10,
    ) {
        pageInfo {
            total
            perPage
            currentPage
            lastPage
            hasNextPage
        }
        media(
            sort: [$sort],
            type: ANIME,
            seasonYear: $seasonYear,
            genre: $genre,
            isAdult: false
        ) {
            ... animeList
        }
    }
}